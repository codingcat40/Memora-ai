# Production: set in Vercel (Project → Settings → Environment Variables).
# Must be the public API origin with no trailing slash.
VITE_API_ORIGIN=https://noema-ai.vercel.app

# Local dev only: optional override for Vite proxy target (see vite.config.ts)
# VITE_PROXY_TARGET=http://127.0.0.1:3000
